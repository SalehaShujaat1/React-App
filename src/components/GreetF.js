import React from "react";
// in functional component no need to define component since it is not going to be extended

const GreetFunctionalComponent = () => <h2>Welcome to Functional Comp: Stateless Comp!</h2>

//default imports
// A few more changes

export default GreetFunctionalComponent;